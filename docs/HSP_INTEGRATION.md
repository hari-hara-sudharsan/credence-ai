# HashKey Settlement Protocol (HSP) Integration Status

**Native HSP Settlement:** ACTIVE (via Abstraction)

Credence AI implements an HSP-native settlement design through the SettlementManager contract.

**Architecture:**
"SettlementManager abstracts HashKey Settlement flow and is designed for HSP execution."

The current architecture provides a unified execution environment for escrowing funds, distributing loans, and enforcing finality on the HashKey Chain.
